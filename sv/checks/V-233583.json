{
  "v_code": "V-233583",
  "severity": "high",
  "rule_id": "SV-233583r961857_rule",
  "rule_ver": "CD12-00-008000",
  "rule_title": "PostgreSQL must implement NIST FIPS 140-2 or 140-3 validated cryptographic modules to generate and validate cryptographic hashes.",
  "check_text": "First, as the system administrator, run the following to see if FIPS is enabled:\n\n$ cat /proc/sys/crypto/fips_enabled\n\nIf fips_enabled is not \"1\", this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "First, as the system administrator, run the following to see if FIPS is enabled:",
      "commands": [
        "cat /proc/sys/crypto/fips_enabled"
      ],
      "finding_condition": null
    }
  ]
}