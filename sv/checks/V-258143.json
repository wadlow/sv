{
  "v_code": "V-258143",
  "severity": "medium",
  "rule_id": "SV-258143r1184329_rule",
  "rule_ver": "RHEL-09-652025",
  "rule_title": "RHEL 9 must be configured so that the rsyslog daemon does not accept log messages from other servers unless the server is being used for log aggregation.",
  "check_text": "Note: If the system administrator (SA) can demonstrate that another tool (e.g., SPLUNK) is being used to manage log off-load and aggregation in lieu of rsyslog, this is not applicable.\n\nVerify RHEL 9 is not configured to receive remote logs using rsyslog with the following command:\n\n$ ss -tulnp | grep rsyslog\n\nIf no output is returned, rsyslog is not listening for remote messages and is compliant.\n\nIf output appears, check for configured ports (514 is the default for syslog).\n\nCheck for remote logging configuration in rsyslog by examining the rsyslog configuration files:\n\n$ sudo grep -E 'InputTCPServerRun|UDPServerRun|RELPServerRun|imtcp|imudp|imrelp' /etc/rsyslog.conf /etc/rsyslog.d/*\n\nIf this command returns uncommented lines enabling network listeners, the system is accepting remote logs.  If this system is not documented and authorized as a log aggregation server, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Note: If the system administrator (SA) can demonstrate that another tool (e.g., SPLUNK) is being used to manage log off-load and aggregation in lieu of rsyslog, this is not applicable.\nVerify RHEL 9 is not configured to receive remote logs using rsyslog with the following command:",
      "commands": [
        "ss -tulnp | grep rsyslog"
      ],
      "finding_condition": null
    },
    {
      "section": null,
      "description": "If no output is returned, rsyslog is not listening for remote messages and is compliant.\nIf output appears, check for configured ports (514 is the default for syslog).\nCheck for remote logging configuration in rsyslog by examining the rsyslog configuration files:",
      "commands": [
        "sudo grep -E 'InputTCPServerRun|UDPServerRun|RELPServerRun|imtcp|imudp|imrelp' /etc/rsyslog.conf /etc/rsyslog.d/*"
      ],
      "finding_condition": null
    }
  ]
}