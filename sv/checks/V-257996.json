{
  "v_code": "V-257996",
  "severity": "medium",
  "rule_id": "SV-257996r1134915_rule",
  "rule_ver": "RHEL-09-255100",
  "rule_title": "RHEL 9 must be configured so that all network connections associated with SSH traffic are terminated after 10 minutes of becoming unresponsive.",
  "check_text": "Verify the \"ClientAliveInterval\" variable is set to a value of \"600\" or less by performing the following command:\n\n$ sudo /usr/sbin/sshd -dd 2>&1 | awk '/filename/ {print $4}' | tr -d '\\r' | tr '\\n' ' ' | xargs sudo grep -iH '^\\s*clientaliveinterval'\n\n/etc/ssh/sshd_config.d/ClientAliveInterval.conf:ClientAliveInterval 600\n\nVerify the runtime value of \"ClientAliveInterval\" with the following command:\n\n$ sudo sshd -T | grep -i clientaliveinterval\n\nclientaliveinterval 600\n\nIf \"ClientAliveInterval\" does not exist, does not have a value of \"600\" or less in \"/etc/ssh/sshd_config\", or is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify the \"ClientAliveInterval\" variable is set to a value of \"600\" or less by performing the following command:",
      "commands": [
        "sudo /usr/sbin/sshd -dd 2>&1 | awk '/filename/ {print $4}' | tr -d '\\r' | tr '\\n' ' ' | xargs sudo grep -iH '^\\s*clientaliveinterval'"
      ],
      "finding_condition": null
    },
    {
      "section": null,
      "description": "/etc/ssh/sshd_config.d/ClientAliveInterval.conf:ClientAliveInterval 600\nVerify the runtime value of \"ClientAliveInterval\" with the following command:",
      "commands": [
        "sudo sshd -T | grep -i clientaliveinterval"
      ],
      "finding_condition": null
    }
  ]
}