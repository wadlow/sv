{
  "v_code": "V-257921",
  "severity": "medium",
  "rule_id": "SV-257921r1106308_rule",
  "rule_ver": "RHEL-09-232205",
  "rule_title": "RHEL 9 library files must be group-owned by root or a system account.",
  "check_text": "Verify the systemwide shared library files contained in the directories \"/lib\", \"/lib64\", \"/usr/lib\", and \"/usr/lib64\" are group owned by root with the following command:\n\n$ sudo find /lib /lib64 /usr/lib /usr/lib64 -type f -name '*.so*' ! -group root -exec stat -c \"%n %G\" {} +\n\nIf any output is returned, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify the systemwide shared library files contained in the directories \"/lib\", \"/lib64\", \"/usr/lib\", and \"/usr/lib64\" are group owned by root with the following command:",
      "commands": [
        "sudo find /lib /lib64 /usr/lib /usr/lib64 -type f -name '*.so*' ! -group root -exec stat -c \"%n %G\" {} +"
      ],
      "finding_condition": null
    }
  ]
}