{
  "v_code": "V-257884",
  "severity": "medium",
  "rule_id": "SV-257884r1106306_rule",
  "rule_ver": "RHEL-09-232020",
  "rule_title": "RHEL 9 library files must have mode 755 or less permissive.",
  "check_text": "Verify the systemwide shared library files contained in the directories \"/lib\", \"/lib64\", \"/usr/lib\", and \"/usr/lib64\" have mode 0755 or less permissive.\n\nCheck that the systemwide shared library files have mode 0755 or less permissive with the following command:\n\n$ sudo find /lib /lib64 /usr/lib /usr/lib64 -type f -name '*.so*' -perm /022 -exec stat -c \"%n %a\" {} +\n\nIf any output is returned, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify the systemwide shared library files contained in the directories \"/lib\", \"/lib64\", \"/usr/lib\", and \"/usr/lib64\" have mode 0755 or less permissive.\nCheck that the systemwide shared library files have mode 0755 or less permissive with the following command:",
      "commands": [
        "sudo find /lib /lib64 /usr/lib /usr/lib64 -type f -name '*.so*' -perm /022 -exec stat -c \"%n %a\" {} +"
      ],
      "finding_condition": null
    }
  ]
}