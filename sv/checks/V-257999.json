{
  "v_code": "V-257999",
  "severity": "medium",
  "rule_id": "SV-257999r1155686_rule",
  "rule_ver": "RHEL-09-255115",
  "rule_title": "RHEL 9 SSH server configuration files' permissions must not be modified.",
  "check_text": "Verify the permissions of the \"/etc/ssh/sshd_config\" file with the following command:\n\n$ sudo rpm --verify openssh-server | awk '! ($2 == \"c\" && $1 ~ /^.\\..\\.\\.\\.\\..\\./) {print $0}'\n\nIf the command returns any output, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify the permissions of the \"/etc/ssh/sshd_config\" file with the following command:",
      "commands": [
        "sudo rpm --verify openssh-server | awk '! ($2 == \"c\" && $1 ~ /^.\\..\\.\\.\\.\\..\\./) {print $0}'"
      ],
      "finding_condition": null
    }
  ]
}