{
  "v_code": "V-258166",
  "severity": "medium",
  "rule_id": "SV-258166r1045303_rule",
  "rule_ver": "RHEL-09-653085",
  "rule_title": "RHEL 9 audit log directory must be owned by root to prevent unauthorized read access.",
  "check_text": "Verify the audit logs directory is owned by \"root\". \n\nDetermine where the audit logs are stored with the following command:\n\n$ sudo grep -iw log_file /etc/audit/auditd.conf\n\nlog_file = /var/log/audit/audit.log\n\nUsing the location of the audit log file, determine if the audit log directory is owned by \"root\" using the following command:\n\n$ sudo stat -c '%U %n' /var/log/audit\n\nroot /var/log/audit\n\nIf the audit log directory is not owned by \"root\", this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify the audit logs directory is owned by \"root\".\nDetermine where the audit logs are stored with the following command:",
      "commands": [
        "sudo grep -iw log_file /etc/audit/auditd.conf"
      ],
      "finding_condition": null
    },
    {
      "section": null,
      "description": "log_file = /var/log/audit/audit.log\nUsing the location of the audit log file, determine if the audit log directory is owned by \"root\" using the following command:",
      "commands": [
        "sudo stat -c '%U %n' /var/log/audit"
      ],
      "finding_condition": null
    }
  ]
}