{
  "v_code": "V-257881",
  "severity": "medium",
  "rule_id": "SV-257881r1184299_rule",
  "rule_ver": "RHEL-09-231200",
  "rule_title": "RHEL 9 must prevent special devices on non-root local partitions.",
  "check_text": "Verify all nonroot local partitions are mounted with the \"nodev\" option with the following command:\n\n$ sudo mount | grep '^/dev\\S* on /\\S' | grep --invert-match 'nodev'\n\nIf any output is produced, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify all nonroot local partitions are mounted with the \"nodev\" option with the following command:",
      "commands": [
        "sudo mount | grep '^/dev\\S* on /\\S' | grep --invert-match 'nodev'"
      ],
      "finding_condition": null
    }
  ]
}