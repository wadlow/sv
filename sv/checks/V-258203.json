{
  "v_code": "V-258203",
  "severity": "medium",
  "rule_id": "SV-258203r1045394_rule",
  "rule_ver": "RHEL-09-654145",
  "rule_title": "RHEL 9 must audit all uses of the su command.",
  "check_text": "Verify that RHEL 9 is configured to audit the execution of the \"su\" command with the following command:\n\n$ sudo auditctl -l | grep '/usr/bin/su\\b'\n\n-a always,exit -S all -F path=/usr/bin/su -F perm=x -F auid>=1000 -F auid!=-1 -F key=privileged-priv_change\n\nIf the command does not return a line, or the line is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify that RHEL 9 is configured to audit the execution of the \"su\" command with the following command:",
      "commands": [
        "sudo auditctl -l | grep '/usr/bin/su\\b'"
      ],
      "finding_condition": null
    }
  ]
}