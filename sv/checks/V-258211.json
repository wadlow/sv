{
  "v_code": "V-258211",
  "severity": "medium",
  "rule_id": "SV-258211r1045418_rule",
  "rule_ver": "RHEL-09-654185",
  "rule_title": "Successful/unsuccessful uses of the init command in RHEL 9 must generate an audit record.",
  "check_text": "Verify that RHEL 9 is configured to audit the execution of the \"init\" command with the following command:\n\n$ sudo auditctl -l | grep /usr/sbin/init\n\n-a always,exit -S all -F path=/usr/sbin/init -F perm=x -F auid>=1000 -F auid!=-1 -F key=privileged-init\n\nIf the command does not return a line, or the line is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify that RHEL 9 is configured to audit the execution of the \"init\" command with the following command:",
      "commands": [
        "sudo auditctl -l | grep /usr/sbin/init"
      ],
      "finding_condition": null
    }
  ]
}