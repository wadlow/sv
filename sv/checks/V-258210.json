{
  "v_code": "V-258210",
  "severity": "medium",
  "rule_id": "SV-258210r1045415_rule",
  "rule_ver": "RHEL-09-654180",
  "rule_title": "RHEL 9 must audit all uses of the mount command.",
  "check_text": "Verify that RHEL 9 is configured to audit the execution of the \"mount\" command with the following command:\n\n$ sudo auditctl -l | grep /usr/bin/mount\n\n-a always,exit -S all -F path=/usr/bin/mount -F perm=x -F auid>=1000 -F auid!=-1 -F key=privileged-mount\n\nIf the command does not return a line, or the line is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify that RHEL 9 is configured to audit the execution of the \"mount\" command with the following command:",
      "commands": [
        "sudo auditctl -l | grep /usr/bin/mount"
      ],
      "finding_condition": null
    }
  ]
}