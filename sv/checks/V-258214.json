{
  "v_code": "V-258214",
  "severity": "medium",
  "rule_id": "SV-258214r1045427_rule",
  "rule_ver": "RHEL-09-654200",
  "rule_title": "Successful/unsuccessful uses of the shutdown command in RHEL 9 must generate an audit record.",
  "check_text": "Verify that RHEL 9 is configured to audit the execution of the \"shutdown\" command with the following command:\n\n$ sudo cat /etc/audit/rules.d/* | grep shutdown\n\n-a always,exit -S all -F path=/usr/sbin/shutdown -F perm=x -F auid>=1000 -F auid!=-1 -F key=privileged-shutdown\n\nIf the command does not return a line, or the line is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify that RHEL 9 is configured to audit the execution of the \"shutdown\" command with the following command:",
      "commands": [
        "sudo cat /etc/audit/rules.d/* | grep shutdown"
      ],
      "finding_condition": null
    }
  ]
}