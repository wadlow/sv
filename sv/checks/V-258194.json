{
  "v_code": "V-258194",
  "severity": "medium",
  "rule_id": "SV-258194r1045367_rule",
  "rule_ver": "RHEL-09-654100",
  "rule_title": "RHEL 9 must audit all uses of the gpasswd command.",
  "check_text": "Verify that RHEL 9 is configured to audit the execution of the \"gpasswd\" command with the following command:\n\n$ sudo auditctl -l | grep gpasswd\n\n-a always,exit -S all -F path=/usr/bin/gpasswd -F perm=x -F auid>=1000 -F auid!=-1 -F key=privileged-gpasswd\n\nIf the command does not return a line, or the line is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify that RHEL 9 is configured to audit the execution of the \"gpasswd\" command with the following command:",
      "commands": [
        "sudo auditctl -l | grep gpasswd"
      ],
      "finding_condition": null
    }
  ]
}