{
  "v_code": "V-257923",
  "severity": "medium",
  "rule_id": "SV-257923r1044991_rule",
  "rule_ver": "RHEL-09-232215",
  "rule_title": "RHEL 9 library directories must be group-owned by root or a system account.",
  "check_text": "Verify the systemwide shared library directories are group-owned by \"root\" with the following command:\n\n$ sudo find /lib /lib64 /usr/lib /usr/lib64 ! -group root -type d -exec stat -c \"%G %n\" {} \\;\n\nIf any systemwide shared library directory is returned and is not group-owned by a required system account, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify the systemwide shared library directories are group-owned by \"root\" with the following command:",
      "commands": [
        "sudo find /lib /lib64 /usr/lib /usr/lib64 ! -group root -type d -exec stat -c \"%G %n\" {} \\;"
      ],
      "finding_condition": null
    }
  ]
}