{
  "v_code": "V-258176",
  "severity": "medium",
  "rule_id": "SV-258176r1155595_rule",
  "rule_ver": "RHEL-09-654010",
  "rule_title": "RHEL 9 must audit uses of the \"execve\" system call.",
  "check_text": "Verify RHEL 9 is configured to audit the execution of the \"execve\" system call with the following command:\n\n$ sudo auditctl -l | grep execve\n\n-a always,exit -F arch=b32 -S execve -C uid!=euid -F euid=0 -k execpriv\n-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -k execpriv\n-a always,exit -F arch=b32 -S execve -C gid!=egid -F egid=0 -k execpriv\n-a always,exit -F arch=b64 -S execve -C gid!=egid -F egid=0 -k execpriv\n\nIf the command does not return all lines, or the lines are commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify RHEL 9 is configured to audit the execution of the \"execve\" system call with the following command:",
      "commands": [
        "sudo auditctl -l | grep execve"
      ],
      "finding_condition": null
    }
  ]
}