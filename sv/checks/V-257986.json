{
  "v_code": "V-257986",
  "severity": "high",
  "rule_id": "SV-257986r1045030_rule",
  "rule_ver": "RHEL-09-255050",
  "rule_title": "RHEL 9 must enable the Pluggable Authentication Module (PAM) interface for SSHD.",
  "check_text": "Verify the RHEL 9 SSHD is configured to allow for the UsePAM interface with the following command:\n\n$ sudo /usr/sbin/sshd -dd 2>&1 | awk '/filename/ {print $4}' | tr -d '\\r' | tr '\\n' ' ' | xargs sudo grep -iH '^\\s*usepam'\n\nUsePAM yes\n\nIf the \"UsePAM\" keyword is set to \"no\", is missing, or is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify the RHEL 9 SSHD is configured to allow for the UsePAM interface with the following command:",
      "commands": [
        "sudo /usr/sbin/sshd -dd 2>&1 | awk '/filename/ {print $4}' | tr -d '\\r' | tr '\\n' ' ' | xargs sudo grep -iH '^\\s*usepam'"
      ],
      "finding_condition": null
    }
  ]
}