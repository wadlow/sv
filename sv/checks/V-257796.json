{
  "v_code": "V-257796",
  "severity": "low",
  "rule_id": "SV-257796r1044847_rule",
  "rule_ver": "RHEL-09-212055",
  "rule_title": "RHEL 9 must enable auditing of processes that start prior to the audit daemon.",
  "check_text": "Verify that GRUB 2 is configured to enable auditing of processes that start prior to the audit daemon with the following commands:\n\nCheck that the current GRUB 2 configuration enables auditing:\n\n$ sudo grubby --info=ALL | grep args | grep -v 'audit=1'\n\nIf any output is returned, this is a finding.\n\nCheck that auditing is enabled by default to persist in kernel updates: \n\n$ grep audit /etc/default/grub\n\nGRUB_CMDLINE_LINUX=\"audit=1\"\n\nIf \"audit\" is not set to \"1\", is missing, or is commented out, this is a finding.",
  "checks": [
    {
      "section": null,
      "description": "Verify that GRUB 2 is configured to enable auditing of processes that start prior to the audit daemon with the following commands:\nCheck that the current GRUB 2 configuration enables auditing:",
      "commands": [
        "sudo grubby --info=ALL | grep args | grep -v 'audit=1'"
      ],
      "finding_condition": null
    },
    {
      "section": null,
      "description": "Check that auditing is enabled by default to persist in kernel updates:",
      "commands": [
        "grep audit /etc/default/grub"
      ],
      "finding_condition": null
    }
  ]
}